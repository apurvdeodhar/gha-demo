name: test

on:
  workflow_run:
    workflows:
      - "build"
    types:
      - "completed"
    branches:
      - '*'
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Print from test
        run: |
          echo "This is from test"
